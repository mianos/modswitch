# ESP-IDF Partition Table — 4MB flash, dual-OTA layout.
#
# The point of this table is that the board only has to be flashed over the
# wire once. After that every update goes over Wi-Fi via POST /firmware, which
# matters here more than usual: this board's USB-serial adapter has no DTR to
# IO0, so entering download mode means a jumper and a power cycle every time.
#
# nvs keeps its standard offset so Wi-Fi credentials and settings survive an
# OTA partition swap. otadata takes the slot phy_init normally occupies at
# 0xf000; phy_init shifts to 0x11000. Two 1.875MB app slots against a ~1MB
# image leave generous headroom. The first OTA after a wired single-app flash
# lands in ota_0 via esp_ota_get_next_update_partition().
# Name,     Type, SubType, Offset,    Size,     Flags
nvs,        data, nvs,     0x9000,    0x6000,
otadata,    data, ota,     0xf000,    0x2000,
phy_init,   data, phy,     0x11000,   0x1000,
ota_0,      app,  ota_0,   0x20000,   0x1E0000,
ota_1,      app,  ota_1,   0x200000,  0x1E0000,
